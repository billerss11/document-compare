interface:
  display_name: "Document Compare"
  short_description: "LLM-ready Beyond Compare reports"
  default_prompt: "Use $document-compare to compare these files with JSONL output and summarize the changes."
