interface:
  display_name: "Document Compare"
  short_description: "Headless Beyond Compare reports for documents."
  default_prompt: "Use $document-compare to compare these two documents and generate a simple report."
